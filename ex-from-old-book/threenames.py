import mytitle

a = 'parrot man'
b = 'blobs'
c = 'green eggs'
print(a, b, c)
titleOf = mytitle.title # Here when you import a module and want to use a function, you can assign it to a variable.
titleOf() #You can then call the attribute into a function.
