import sys
print(sys.platform)
print(2 ** 100)
x = 'Spam! '
print(x * 8)
# to place the output onto a file for later review, simply
# use to command line command of "file.py > file.txt"
input(); # this is just an input call so that the script won't

nextinput = input('Place your card info here please!');
thisInput = input('python rand-print.py < input.txt');
print(thisInput);
eval(input('gimme stuff'))
